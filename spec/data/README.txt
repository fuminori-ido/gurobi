Data files here, which are used to run 'rake spec', was copied from
$GUROBI_HOME/examples/data/, but are intentionally removed because of
copyright.

If you need to run 'rake spec', please copy them as follows:

  $ cp $GUROBI_HOME/examples/data/* [DIR_HERE]
